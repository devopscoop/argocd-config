# Arch Linux package list for argocd-config — the pacman/AUR equivalent of the
# Brewfile. Installs every CLI tool used or referenced by this repo.
#
# All packages are in the official repos, so plain pacman works (as do yay/paru):
#
# Usage:
#   grep -vE '^(#|$)' pkglist.txt | sudo pacman -S --needed -

# helm — installing/upgrading ArgoCD from the Helm chart with values.yaml
helm

# kubectl — registering clusters, creating secrets, troubleshooting
kubectl
